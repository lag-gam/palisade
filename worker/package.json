{
  "name": "agentfence-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute agentfence-db --local --file=src/db/schema.sql",
    "db:seed:local": "wrangler d1 execute agentfence-db --local --file=src/db/seed.sql",
    "db:migrate": "wrangler d1 execute agentfence-db --file=src/db/schema.sql",
    "db:seed": "wrangler d1 execute agentfence-db --file=src/db/seed.sql"
  },
  "dependencies": {
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "wrangler": "^3.60.0",
    "typescript": "^5.5.0"
  }
}
